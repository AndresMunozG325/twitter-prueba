<!--<nav class="navbar navbar-expand-lg navbar-dark bg-dark">-->
<nav class="navbar navbar-expand-md navbar-dark fixed-top bg-primary">

    <div class="container">
        <a class="navbar-brand" href="<%= root_path%>"><%=t('Twitter')%></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarText">
            <ul class="navbar-nav ml-auto">
              <% if user_signed_in? %> 
                  <p id="p_name"> Hi  <%=current_user.name %> </p>
              <% else %>
                  <p id="p_name"> Hi  Unknow </p>   
              <% end %>
              <% if user_signed_in? %>
                  <li class="nav-item">
                  <%= link_to 'Log Out', destroy_user_session_path, method: :delete, class:'nav-link text-white'%>
                  </li>
              <%else%>
                  <li class="nav-item active">
                  <%= link_to 'Sign Up', new_user_registration_path, class:'nav-link text-white'%>
                  </li>
                  <li class="nav-item">
                  <%= link_to 'Log In', new_user_session_path, class:'nav-link text-white'%>
                  </li>
              <%end%>

            </ul>
                
                
                <%= form_tag('/search', options = { class: "d-flex" }) do%>
                    <%= text_field_tag("term", value = nil, options = {class: "form-control me-2", placeholder: "Search"})  %>
                    <%= submit_tag(value = 'Search', options = { class: "btn btn-outline-primary" }) do%>
                        <i class="fas fa-search fa-sm"></i>
                    <% end %> 
                <% end %> 
                
             
        </div>
    </div>
</nav>